services:
  - type: web
    name: solana-trader
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PHANTOM_PRIVATE_KEY
value: